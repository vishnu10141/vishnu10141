<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 900 70" width="900" height="70">
  <defs>
    <linearGradient id="ft-top-grad" x1="0%" y1="0%" x2="100%" y2="0%">
      <stop offset="0%" stop-color="#1A2540" stop-opacity="0"/>
      <stop offset="50%" stop-color="#1A2540" stop-opacity="1"/>
      <stop offset="100%" stop-color="#1A2540" stop-opacity="0"/>
    </linearGradient>
  </defs>

  <!-- Background -->
  <rect width="900" height="70" fill="#050A12"/>

  <!-- Top border line -->
  <rect x="40" y="0" width="820" height="0.8" fill="url(#ft-top-grad)"/>

  <!-- Left: Name -->
  <text x="40" y="42" fill="#3D4F70" font-family="monospace" font-size="10" letter-spacing="2" opacity="0.6">V.NIMMAKAYALA</text>

  <!-- Left: Sub -->
  <text x="40" y="58" fill="#3D4F70" font-family="monospace" font-size="9" opacity="0.4">Applied ML Engineer</text>

  <!-- Center decoration dots -->
  <circle cx="450" cy="35" r="1.5" fill="#1A2540" opacity="0.5"/>
  <circle cx="450" cy="42" r="1.5" fill="#1A2540" opacity="0.5"/>
  <circle cx="450" cy="49" r="1.5" fill="#1A2540" opacity="0.5"/>

  <!-- Right: Credit -->
  <text x="860" y="36" text-anchor="end" fill="#3D4F70" font-family="monospace" font-size="9" opacity="0.5">2025 · Built with GitHub Markdown &amp; SVG</text>

  <!-- Right: Location -->
  <text x="860" y="52" text-anchor="end" fill="#3D4F70" font-family="monospace" font-size="9" opacity="0.4">Hyderabad, India</text>
</svg>
